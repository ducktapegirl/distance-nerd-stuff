Run the unified `/dashboard` orchestrator workflow with the target **`running-log`**.

`/running-log` is a thin alias — the pipeline lives in `.claude/commands/dashboard.md` and
serves both dashboards. Follow that command's instructions exactly, treating the target as
`running-log` and forwarding any remaining arguments as the mode (e.g. `/running-log
maintenance` → `/dashboard running-log maintenance`).

$ARGUMENTS
