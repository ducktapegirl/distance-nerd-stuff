[project]
name = "distance-nerd-stuff"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "plotly",
    "numpy",
    "requests",
    "python-dotenv",
    "tqdm",
    "beautifulsoup4",
]
