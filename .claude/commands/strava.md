Run the unified `/dashboard` orchestrator workflow with the target **`strava-data`**.

`/strava` is a thin back-compat alias — the pipeline now lives in `.claude/commands/dashboard.md`
and serves both dashboards. Follow that command's instructions exactly, treating the target as
`strava-data` and forwarding any remaining arguments as the mode (e.g. `/strava maintenance`
→ `/dashboard strava-data maintenance`).

$ARGUMENTS
